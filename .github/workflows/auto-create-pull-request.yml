name: (Auto) Create Pull Request

on:
  push:
    branches-ignore:
      - master
      - dependabot/**

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  call-auto-create-pull-request:
    uses: devops-infra/.github/templates/other/workflows/auto-create-pull-request.yml@v1
    with:
      runs-on: ubuntu-24.04-arm
      task-version: 3.x
      enable-docker: false
      enable-lint: true
      docker-platforms: amd64,arm64
